services:
  tracker:
    build: .
    container_name: jeonghoo_tracker
    restart: unless-stopped
    network_mode: host
    
    # .env 파일에서 환경변수 로드
    env_file:
      - .env
    
    volumes:
      # 개발용: 전체 폴더 마운트 (코드 수정 즉시 반영, 재시작만 필요)
      - ./:/app
